<h2>{{SectionTitle}}</h2>
<table>
  {{#each entries}}
    <tr>
      <td style="text-align: left; padding: 20px 25px 20px 25px;">
        {{{Authors}}}
        {{Title}}.
        <b><i>{{Publication}}</i></b>
      </td>
    </tr>
  {{/each}}
</table>
