<h2>{{SectionTitle}}</h2>
<table>
  {{#each entries}}
    <tr>
      <td style="text-align: left; padding: 20px 25px 20px 25px; font-size: 14px;">
        {{{Authors}}}
        <a href="{{URL}}" target="_blank">{{Title}}.</a>
        <b><i>{{Publication}}</i></b> {{Location}}
      </td>
    </tr>
  {{/each}}
</table>
