<h2>{{SectionTitle}}</h2>
<table>
  {{#each entries}}
      <tr>
        <td style="text-align: left;">
          <div class="avoid">
            <b>{{Title}}</b><br>
            <span style="font-size: 12px;">{{Authors}}</span>
          </div>
        </td>
        <td style="font-size: 12px; text-align: right">
          {{What}} - {{Where}}
          <br>
          {{When}}
        </td>
      </tr>
  {{/each}}
</table>
